---
import type { SelectOptions } from '@/types'

interface Props {
  id: string
  label: string
  placeholder?: string
  isRequired?: boolean
  options: SelectOptions[]
  class?: string
}

const {
  id,
  label,
  placeholder = 'Select an option',
  isRequired = false,
  options,
  class: className,
} = Astro.props
---

<div class:list={className}>
  <label for={id} class="block mb-2 text-sm font-medium text-white">
    {label}
  </label>
  <select
    id={id}
    class="border text-sm rounded-lg block w-full p-2.5 bg-hawaii-input border-hawaii-steel placeholder-slate-500 text-white focus:ring-amber-500 focus:border-amber-500"
    required={isRequired}
  >
    <option value="" disabled selected>{placeholder}</option>
    {
      options.map((option) => (
        <option value={option.value}>{option.text}</option>
      ))
    }
  </select>
</div>
